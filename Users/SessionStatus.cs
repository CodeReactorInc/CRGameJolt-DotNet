namespace CodeReactor.CRGameJolt.Users
{
    /// <summary>
    /// Represent the session status in GameJolt Game API
    /// </summary>
    public enum SessionStatus
    {
        /// <value>
        /// Sets the session to the active state.
        /// </value>
        Active,
        /// <value>
        /// Sets the session to the idle state.
        /// </value>
        Idle
    }
}
